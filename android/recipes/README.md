# Private python-for-android recipes

This directory is reserved for dependencies that need Android-specific build
logic. The planned `croc` integration belongs here after its Android binary and
runtime behaviour have been validated. Keeping recipes local prevents changes
to the host's global python-for-android installation.
